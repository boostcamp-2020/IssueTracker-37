import styled from 'styled-components';

export const StyledSimpleNavbar = styled.nav`
  display: flex;
  width: 100%;
  justify-content: space-between;

  & > .navBtn {
    flex: none;
  }
  & > .newIssueBtn {
    flex: none;
  }
`;
